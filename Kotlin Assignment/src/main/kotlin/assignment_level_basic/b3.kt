// Kotlin Program to Print an Integer (Entered by the User)//

package assignment_level_basic

import java.util.Scanner

fun main() {
    val scananner = Scanner(System.`in`)
    print("Enter Value:")

    val no = scananner.nextInt()
    println("VALUE:$no")
}